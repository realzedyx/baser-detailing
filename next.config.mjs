/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js image optimization is enabled (resizing + AVIF/WebP). Use next/image
  // for any photography (e.g. before/after gallery) to get it for free.
};

export default nextConfig;
